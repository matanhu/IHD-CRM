import { ContactTooltipDirective } from './contact-tooltip.directive';

describe('ContactTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new ContactTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
